## What

PR #25 实机验证发现两个问题（用户反馈 2026-08-24）：
1. Cmd+Shift+P 打开的是 Obsidian 核心的 quick switcher（打开文件列表）——核心绑定遮蔽插件默认热键，新建项目弹窗从未打开
2. 新建任务单输入框依赖捕获语法 `@项目`，不写就落到 `_未分类`——错误的项目路径

## Changes

- **热键改绑 P→J**：new-project 改 Mod+Shift+J（N 不动；J 与核心/本插件均无冲突）
- **新建任务改表单弹窗**：
  - 标题（必填）
  - 项目：datalist 下拉列出 knownProjects（store 派生，wikilink 剥壳，与 `03 Tasks/` 磁盘文件夹 1:1）；可手输新名；留空 = `_未分类`
  - 优先级：下拉 high/normal/low/无
  - 到期：NL 文本（明天下午3点/后天/周五/ISO），**实时解析预览**；留空 = 今天 22:00（既有默认规则）；解析失败拒提交
- **formToCapture 纯函数**（src/view/newTaskForm.ts）组装表单值→Capture，仍走 createTaskFile 同一缝：捕获框/命令/表单三入口建档字节一致
- i18n zh/en 表单键 + 表单样式 + spec FR-040/041 修订注记 + SC-022 更新

## Verification

- tsc --noEmit ✅
- vitest 452/452（新增 10：formToCapture 组装/校验、knownProjects 派生去重排序、previewDue、热键改绑）✅
- build ✅
- 实机验证待部署后用户复验（Cmd+Shift+J 弹窗 + 表单建档落对项目路径）

Traces-to: FR-040 (rev), FR-041 (rev), SC-022, H13
